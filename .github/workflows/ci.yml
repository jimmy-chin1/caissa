name: CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: backend-checks
    runs-on: ubuntu-latest

    steps:
        - name: Check out repository
          uses: actions/checkout@v6
